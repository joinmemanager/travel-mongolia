import { NextResponse } from 'next/server';

export async function POST(req: Request) {
  try {
    const body = await req.json();
    return NextResponse.json({ 
      reply: "???? ????? ??! Travel Mongolia ?????? ??? ??????? ????? ??????? ??????." 
    });
  } catch (error) {
    return NextResponse.json({ error: "????? ??????" }, { status: 500 });
  }
}
